[build]
dockerfilePath = "stitch-server/Dockerfile"

[deploy]
startCommand = "node index.mjs"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
